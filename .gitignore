.terraform/
# Commit .terraform.lock.hcl for reproducible provider versions (no secrets in it).
terraform.tfvars
*.tfvars.local
*.auto.tfvars
# Saved deploy inputs (contain quay creds) — never commit.
orm-vars.json
*.tfstate
*.tfstate.*
crash.log
*.yaml.kubeconfig

.terraform.lock.hcl
